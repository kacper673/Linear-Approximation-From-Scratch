#include <iostream>
#include "aprox.h"
#include "UI.h"
int main()
{
    data data1;
    WindowManager window;
    //options
    // 0 -points only
    // 1 - linear aproximation
    // 2 - interpolation of points
    // 3 - both
    window.run(1);
     
}
